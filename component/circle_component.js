export default class CircleComponent {
    constructor() {
        this.radius = 0;
        this.color = 'black';
    }

    init(radius, color) {
        this.radius = radius;
        this.color = color;
    }
}